 <!-- <script lang="ts">
  interface Props {
    tone?: 'cyan' | 'amber' | 'danger' | 'success' | 'muted';
    pulse?: boolean;
  }

  const { tone = 'cyan', pulse = false } = $props<Props>();

  const toneColor = {
    cyan: 'bg-cyan',
    amber: 'bg-amber',
    danger: 'bg-danger',
    success: 'bg-success',
    muted: 'bg-muted'
  }[tone];
</script>

<span class="status-dot {toneColor} {pulse ? 'status-pulse' : ''}" />
-->